// Tang Nano 20K pin constraints for Church Machine
// Gowin GW2AR-LV18QN88C8/I7 — 27MHz crystal, BL616 USB-UART, 4 LEDs, 1 button
// Onboard LEDs are on pins 15–20; pins 17–18 conflict with UART, so 4 usable.

// System clock — 27MHz crystal oscillator
IO_LOC "clk" 4;
IO_PORT "clk" IO_TYPE=LVCMOS33 PULL_MODE=NONE;

// UART via BL616 USB bridge — pin 17 = BL616 TX→FPGA RX, pin 18 = FPGA TX→BL616 RX
IO_LOC "uart_tx" 18;
IO_PORT "uart_tx" IO_TYPE=LVCMOS33 PULL_MODE=NONE DRIVE=8;
IO_LOC "uart_rx" 17;
IO_PORT "uart_rx" IO_TYPE=LVCMOS33 PULL_MODE=UP;

// Onboard LEDs (active-low) — 6 LEDs on pins 15-20, using 4 (pins 17,18 used by UART)
IO_LOC "led0" 15;
IO_PORT "led0" IO_TYPE=LVCMOS33 PULL_MODE=NONE DRIVE=8;
IO_LOC "led1" 16;
IO_PORT "led1" IO_TYPE=LVCMOS33 PULL_MODE=NONE DRIVE=8;
IO_LOC "led2" 19;
IO_PORT "led2" IO_TYPE=LVCMOS33 PULL_MODE=NONE DRIVE=8;
IO_LOC "led3" 20;
IO_PORT "led3" IO_TYPE=LVCMOS33 PULL_MODE=NONE DRIVE=8;

// Push button S1 (active-low)
IO_LOC "push_button" 88;
IO_PORT "push_button" IO_TYPE=LVCMOS33 PULL_MODE=UP;
