⦗OSS CAD Suite⦘ (venv-church) sipantichijk@penguin:~/Downloads/Ti60/church-ti60-package$ sed -n '45,70p' /home/sipantichijk/efinity/2025.2/pt/bin/common_device/gpio/writer/interface_config.py
echo "---"
sed -n '195,215p' /home/sipantichijk/efinity/2025.2/pt/bin/common_device/gpio/gpio_device_service.py
        :return: inf_config: A list of ucp.UsedCorePort
        '''
        tmp_config = []

        if gpio_obj is not None:
            # Skip if the obj is not set on device instance
            # that is not active (available in resource)
            if gpio_obj.gpio_def not in active_instances:
                if str(gpio_obj.gpio_def) in ('GPIOT_P_07_CLK4_P', 'GPIOT_N_07_CLK4_N', 'GPIOT_P_08_CDI_P', 'GPIOT_N_08_CDI_N'):
                    return tmp_config  # 2025.2 patch: skip CLK/CDI pins
                msg = "Resource {} assigned to {} does not exists in device".format(
                    gpio_obj.gpio_def, gpio_obj.name)

                raise dev_excp.InstanceInvalidResourceException(
                    msg, app_excp.MsgLevel.error)

            # Check if it is a bus member. If yes, capture
            # the bus member order
            is_msb_greater_than_lsb = None
            if gpio_obj.bus_name != "":
                gpio_bus = gpio_reg.get_bus_by_name(gpio_obj.bus_name)

                if gpio_bus is not None:
                    is_msb_greater_than_lsb = gpio_bus.is_msb_greater_than_lsb()

            # We operate depending on the Mode type.
---
                # Skip if the obj is not set on device instance
                # that is not active (available in resource)
                if gpio_obj.gpio_def in active_instances:

                    ins2blk[gpio_obj.gpio_def] = self.get_name()

                else:
                    if str(gpio_obj.gpio_def) in ('GPIOT_P_07_CLK4_P', 'GPIOT_N_07_CLK4_N', 'GPIOT_P_08_CDI_P', 'GPIOT_N_08_CDI_N'):
                        continue  # 2025.2 patch: skip CLK/CDI pins
                    msg = "Resource {} assigned to {} does not exists in device".format(
                        gpio_obj.gpio_def, gpio_obj.name)

                    raise dev_excp.InstanceInvalidResourceException(
                        msg, app_excp.MsgLevel.error)

        return ins2blk

    def get_all_instances(self, design):
        '''
        Return the list of user configured instance queried
        from the block's registry.
⦗OSS CAD Suite⦘ (venv-church) sipantichijk@penguin:~/Downloads/Ti60/church-ti60-package$ 