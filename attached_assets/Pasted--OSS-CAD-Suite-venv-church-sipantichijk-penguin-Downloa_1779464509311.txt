⦗OSS CAD Suite⦘ (venv-church) sipantichijk@penguin:~/Downloads/Ti60/church-ti60-package$ sed -n '44,65p' /home/sipantichijk/efinity/2025.2/pt/bin/common_device/gpio/writer/interface_config.py
echo "---"
sed -n '192,215p' /home/sipantichijk/efinity/2025.2/pt/bin/common_device/gpio/writer/interface_config.py
        :param active_instances: Optional: None if not needed
        :return: inf_config: A list of ucp.UsedCorePort
        '''
        tmp_config = []

        if gpio_obj is not None:
            # Skip if the obj is not set on device instance
            # that is not active (available in resource)
            if gpio_obj.gpio_def not in active_instances:
                msg = "Resource {} assigned to {} does not exists in device".format(
                    gpio_obj.gpio_def, gpio_obj.name)

                raise dev_excp.InstanceInvalidResourceException(
                    msg, app_excp.MsgLevel.error)

            # Check if it is a bus member. If yes, capture
            # the bus member order
            is_msb_greater_than_lsb = None
            if gpio_obj.bus_name != "":
                gpio_bus = gpio_reg.get_bus_by_name(gpio_obj.bus_name)

                if gpio_bus is not None:
---
                            is_continue, io_pin_name = self._get_input_reg_conn(input_config, pin_conn)
                            if is_continue:
                                continue

                        # Skip when it is not registered but it has ddio
                        elif pin_conn.get_index() == 1:
                            continue

                elif pin_name == "INCLK":
                    if input_config.conn_type == \
                        input_config.ConnType.normal_conn and\
                            input_config.is_register:
                        clk_name = input_config.clock_name
                        is_clk_inverted = input_config.is_clk_inverted
                        io_pin_name = ''
                    else:
                        continue
                else:
                    continue

                p = self._save_inf_pin(dev_ins=ins_obj,
                                                     user_ins=gpio_obj,
                                                     inf_config=inf_config,
                                                     pin_conn=pin_conn, coord=coord,
⦗OSS CAD Suite⦘ (venv-church) sipantichijk@penguin:~/Downloads/Ti60/church-ti60-package$ 